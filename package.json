{
  "name": "network-monitor",
  "version": "1.1.3",
  "description": "Network Monitor Chrome Extension (Manifest V3, TypeScript)",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "esbuild": "^0.23.1",
    "typescript": "^5.5.4",
    "@types/chrome": "^0.0.270"
  }
}
